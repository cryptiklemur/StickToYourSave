name: ship-list

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    uses: RimWorks/mod-ci/.github/workflows/ship-list.yml@v1.10
    with:
      ref: v1.10
